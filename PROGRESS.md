# Progress

## Confirmed Facts

- This v2 checkout now loads target-side Windows CE/MIPS DLLs as PE images instead of binding every import to a local shim page.
- The loader searches the primary EXE directory plus DLL search directories supplied on the command line; it no longer requires a DLL-copy helper script or hardcoded local SDK paths.
- The loader builds in-memory PE images, maps them into Unicorn, applies base relocations, parses exports, and recursively binds imports. Missing DLLs or unresolved exports are fatal.
- Pre-fallback-removal smoke verification loaded 12 PE modules:
  - `inavi.exe`
  - `AuthLibrary.dll`
  - `mMbcAuth.dll`
  - `tpeg_if_dll.dll`
  - `TpSysAuth.dll`
  - `tw_tpeg_if_dll.dll`
  - `mfcce400.dll`
  - `mfcce400d.dll`
  - `mfcce400i.dll`
  - `atlce400.dll`
  - `olece400.dll`
  - `olece400d.dll`
- Earlier smoke verification bound `inavi.exe` imports with `real=266 fallback=370`, proving MFC imports were going to real SDK DLL exports where available before fallback support was removed.
- After fallback removal, the same explicit SDK search-path smoke run fails fast on missing `COREDLL.dll`; no shim page or fallback import target is installed.
- CLI contract is now `iNavi_Unicorn_Emulator.exe <primary.exe> [dll_search_dir ...]`.
- A search of the installed Windows CE 4.2 Standard SDK under `C:\Program Files (x86)\Windows CE Tools` and the local iNavi tree found `coredll.lib` import libraries but no real `COREDLL.dll` PE image.
- Synthetic `coredll.dll` is now fail-closed for unknown coredll exports. Supported exports are named from Windows CE 4.2 Standard SDK MIPSII `coredll.lib` COFF import-object headers, not from `/LINKERMEMBER` guesses.
- Host-compatible coredll APIs are bridged through translated guest arguments where possible. Opaque handles such as files, events, mutexes, HWNDs, menus, accelerators, resources, and wave input are represented as guest handles mapped to host handles or guest-side records.
- `winmm.dll` is loaded dynamically on the host for wave input bridging. Guest `HWAVEIN` values are mapped handles; guest `WAVEHDR` buffers are copied to host-side `WAVEHDR` storage before host calls.
- The main EXE resource table is parsed from PE resources in `SyntheticDllRuntime::setMainModulePath`. Current verified `INavi.exe` resources: 18 entries, including bitmaps, dialogs, one menu (`RT_MENU` id 128), one group icon (`RT_GROUP_ICON` id 128), version, and CEUX metadata. No `RT_STRING` or `RT_ACCELERATOR` resources were present in the parsed table.
- Resource APIs now use parsed EXE resources: `FindResource/FindResourceW`, `LoadResource`, `SizeofResource`, and `LoadStringW` are resource-table backed. `LoadMenuW` creates a host `HMENU` with `LoadMenuIndirectW`; `RemoveMenu`, `CheckMenuItem`, and `CheckMenuRadioItem` operate on mapped host menu handles.
- `LoadIconW` and `LoadImageW(..., IMAGE_ICON, ...)` translate parsed `RT_GROUP_ICON`/`RT_ICON` resources into host `HICON` objects behind guest handles. `LoadImageW(..., IMAGE_BITMAP, ...)` can translate parsed `RT_BITMAP` DIB data into a host `HBITMAP`; true resource misses still fail closed.
- Guest window state is tracked separately from host HWNDs. `RegisterClassW`, `GetClassInfoW`, `CreateWindowExW`, `FindWindowW`, `GetWindowLongW`, `SetWindowLongW`, `GetParent`, and `GetWindow` use guest HWND records rather than passing guest pointers or fake host HWNDs.
- Default non-headless runs create a host presenter window for the guest framebuffer when a top-level guest HWND appears. If the guest later destroys that HWND, the guest handle is still removed, but the host presenter is retained as an inspection surface and the process enters a host GUI message loop until the presenter is closed. `--headless` skips that presenter loop for bounded smoke runs.
- The message bridge now maps `PostMessageW`, `PostQuitMessage`, `GetMessageW`, `GetMessageWNoWait`, `PeekMessageW`, `TranslateMessage`, `DispatchMessageW`, `SendMessageW`, and `DefWindowProcW`. `DispatchMessageW`/`SendMessageW` transfer into the guest WNDPROC with translated message arguments.
- `DestroyWindow` now dispatches `WM_DESTROY` and `WM_NCDESTROY` synchronously through the guest WNDPROC with a synthetic continuation return. `WM_NCDESTROY` uses the current window procedure after any app/MFC subclass changes made during `WM_DESTROY`. This matches the MFC CE teardown path better than queueing destroy messages asynchronously.
- Guest window visibility is tracked. `ShowWindow` updates visibility and queues `WM_SHOWWINDOW`; `UpdateWindow` queues `WM_ERASEBKGND` followed by `WM_PAINT`; `IsWindowVisible` returns guest window state.
- Empty blocking `GetMessageW` no longer fabricates `WM_NULL`; it stops Unicorn with an explicit "blocking with empty guest queue" log entry so the emulator reports the honest idle state.
- Synthetic `commctrl.dll` is now module-scoped and SDK-ordinal named from Windows CE 4.2 Standard SDK MIPSII `commctrl.lib` COFF import-object headers. The old module-wide "return 1" behavior is removed. CE command-bar/status/toolbar/updown creation returns guest HWND records, command-bar menu insertion uses the parsed resource/host `HMENU` bridge, common-control init functions report success, and unsupported property-sheet display fails closed instead of pretending to show UI.
- Guest heaps and critical sections are tracked as guest-side objects instead of pseudo handles/no-op returns. `WNetGetUserW` is host-backed via `GetUserNameW` and copies through guest memory with Win32-style length/error handling.
- Module dispatch is now module-scoped after DLL name resolution. Synthetic module calls no longer fall through to stale name-only comparisons after the coredll path; `WINSOCK.dll`, `ole32.dll`, and `OLEAUT32.dll` each have their own dispatch path.
- Synthetic `WINSOCK.dll` now bridges directly to host Winsock for startup/cleanup, socket lifecycle, connect/bind/listen/accept, send/recv, select, socket options/ioctls, byte-order helpers, and basic name lookup. Guest socket handles map to host `SOCKET` values.
- Synthetic `ole32.dll` now bridges host-compatible COM helpers through host COM: `CoInitializeEx`, `CoUninitialize`, GUID/CLSID/ProgID conversion, `CoCreateGuid`, and `CoCreateInstance`. Successful host COM interfaces are exposed to the guest as translated proxy objects with guest-side `IUnknown::QueryInterface/AddRef/Release` stubs; host COM pointers are not passed directly into guest memory.
- Synthetic `OLEAUT32.dll` implements guest BSTR allocation/free/length helpers and minimal Variant conversion/clear paths in guest memory.
- Additional coredll window/GDI helpers reached by the current smoke are now implemented with SDK-confirmed or runtime-confirmed behavior: `SetCursor`, `GetDlgCtrlID`, `AdjustWindowRectEx`, `EqualRect`, `CreatePen`, `GetWindowRect`, `ScreenToClient`, `SetForegroundWindow`, `GetActiveWindow`, `IsWindowEnabled`, and `MessageBoxW`.
- COREDLL ordinal `#89` is confirmed from the Windows CE 4.2 Standard SDK MIPSII `coredll.lib` COFF import-object headers as `SystemParametersInfoW`, not `wcslen`. Runtime call shape matched `SystemParametersInfoW(0x102, bufferChars, buffer, 0)`.
- `SystemParametersInfoW` is now implemented as a registry-backed coredll bridge. It reads action values from the external registry JSON under `hklm\system\emulator\systemparametersinfo` by 8-digit hex action or aliases such as `oeminfo`; no device/OEM identity is hardcoded in `.cpp`.
- `KernelIoControl` diagnostic returns are now registry-table backed under `hklm\system\emulator\kernelioctl\<entry>` using `ioctlcmd` for the command and `return` for the result. The bridge writes DWORD returns as guest DWORDs and string returns as raw NUL-terminated bytes because the app-side `0x00298530` path scans the output with `lbu` and then calls `MultiByteToWideChar`; there is no hardcoded success path for `0x01012ef4`.
- The JSON registry database is normalized on load so key paths and value names are case-insensitive (`HKLM`, `HKEY_LOCAL_MACHINE`, mixed-case paths, and mixed-case values fold to the internal canonical form). Registry string values are preserved as UTF-8 in JSON and exposed to the guest as UTF-16LE bytes.
- Smoke71 verified that the current diagnostic registry values are read correctly through `RegQueryValueExW`, including `ModelID`, `DeviceName`, and `SystemParametersInfoW(0x102)`.
- The target now reaches an app-level `MessageBoxW` saying `can't read HWInfoDB`. `INavi\res\values.dat` is opened successfully and read through host-backed file APIs before that message. The current no-draw state is caused by the app aborting before its paint path, not by a missing GUI window creation path.
- iSearch smoke verification is now a useful second-target check. `v2_synth_isearch_smoke05_final.log` launches `C:\Users\royna\Downloads\INAVI\INavi\iSearch.exe`, parses 52 resources from `iSearch.exe`, loads 16 PE modules, creates a top-level guest window titled `iSearch`, translates CE process-slot WNDPROC values such as `0x0604f134 -> 0x0004f134`, and reaches `GetMessageW blocking with empty guest queue` with `UC_ERR_OK`.
- COREDLL ordinals reached by iSearch and implemented from SDK/runtime evidence include `#197=WideCharToMultiByte`, `#548=CreateFileMappingW`, `#640=GetProcessIndexFromID`, `#1102=printf`, and `#1132=_vsnwprintf`. `#1875` is still unresolved in the installed Windows CE 4.2 Standard SDK MIPSII `coredll.lib`; the current runtime returns 0 and iSearch still reaches idle.
- CE_MANAGER contains Windows CE/MIPS GUI binaries at `C:\Users\royna\Downloads\INAVI\CE_MANAGER`: `CE_Manager.exe`, `CE_Manager_AutoUpgrade.exe`, and `CE_Manager_Login.exe`. `CE_Manager.exe` parsed 20 resources but failed before guest execution because `WININET.dll` was not present in the app directory or the explicit SDK search paths. The installed Standard SDK provides `wininet.lib/.exp` for MIPSII, but no real MIPS `WININET.dll` PE image was found.
- The user supplied a real-device dump at `C:\Users\royna\Downloads\DUMPPLZ`; treat it as the current source of truth for device identity and filesystem facts. Confirmed dump facts: module path `\SDMMC Disk\iNavi\iNavi.exe`, screen `800x480`, CPU arch `1` type `4000`, existing roots `\Windows`, `\ResidentFlash`, `\SDMMC DISK`, and registry identity `HKLM\Ident\Name = "iNavi GN 2010"` / `Desc = "Navigation"`.
- `dumpplz_regs.json` was generated from `DUMPPLZ\REGISTRY.TXT` and adds only emulator-control registry inputs derived from the dump identity: `SystemParametersInfoW(0x102)` / `oeminfo = "iNavi GN 2010"`, `KernelIoControl(0x01012ef4)` string return `"iNavi GN 2010"`, and a provisional external `KernelIoControl(0x0101207c)` DWORD return of `0` pending a real buffer contract.
- Dump-backed smoke with the INAVI payload root before `DUMPPLZ\FILES` reaches the main window, plays startup audio, draws the right-side toolbar/icons, runs `TransparentImage` once successfully on the splash/window path, and stops cleanly at `GetMessageW blocking with empty guest queue`. A dump-only filesystem root fails earlier with `can't create iNaviData folder`, showing that root enumeration/mount merging still needs real CE-style volume behavior.

## Latest Verification

- MSBuild succeeded after the `SystemParametersInfoW` ordinal correction, registry JSON normalization, and UTF-16 registry string changes.
- MSBuild and smoke `v2_synth_inavi_ioctl_ansi_trace.log` succeeded after correcting registry-backed `KernelIoControl` string returns to raw NUL-terminated bytes. The run returned DWORD `0` for `0x0101207c`, wrote `"iNavi M31 2007"` as 15 narrow bytes for `0x01012ef4`, and `MultiByteToWideChar` converted the full string instead of only the first character. The app still reaches the `can't read HWInfoDB` message afterward.
- Assembly diagnostic smoke `v2_synth_inavi_hwinfo_asm_diag.log` shows the current failure point: the HWInfo profile matcher leaves the singleton at id `0`, subId `0`, name empty; the app then calls the `values.dat` lookup with requested id `0`, scans 118 records, and misses because the table ids are 1..118. This points to diagnostic identity/profile mismatch before record lookup, not a failed `values.dat` file open/read.
- Final bounded smoke `v2_synth_inavi_hwinfo_reason_final.log` after diagnostic cleanup and rebuild confirms the same normal-run state: `KernelIoControl(0x01012ef4)` returns raw `"iNavi M31 2007"` bytes, `MultiByteToWideChar` returns 15, `values.dat` lookup misses requested id `0` after scanning 118 records, then the app displays `can't read HWInfoDB` and cleanly idles.
- Smoke `v2_synth_system_smoke71_registry_utf8.log` with explicit SDK DLL directory arguments, `--registry .\diag_regs_hwinfo_m31_probe.json`, and `--headless` parsed resources, loaded the real SDK/user DLL set where present, mapped synthetic `commctrl.dll`, `WINSOCK.dll`, `ole32.dll`, and `OLEAUT32.dll`, read the diagnostic registry values, opened/read `INavi\res\values.dat`, and stopped cleanly at `GetMessageW blocking with empty guest queue`, `UC_ERR_OK pc=0x70002ae8 ra=0x500245c8`.
- Smoke71 has no `unsupported by translate layer` calls before the idle stop. The remaining visible failure is the app-level `can't read HWInfoDB` message.
- MSBuild also succeeded before the iSearch smoke test. Smoke `v2_synth_isearch_smoke05_final.log` with `--registry .\diag_regs_hwinfo_probe.json`, `--headless`, and explicit MIPSII SDK MFC/ATL search paths reached the iSearch idle message wait. The only remaining unsupported call in that run is unresolved COREDLL ordinal `#1875`, which did not block launch.
- MSBuild succeeded before the CE_MANAGER probe. Smoke `v2_synth_cemanager_smoke01.log` mapped `CE_Manager.exe` and then failed fast with `required DLL not found: WININET.dll` before `starting Unicorn`.
- MSBuild succeeded after implementing the real GDI-backed `TransparentImage` path. Dump-backed smoke `v2_synth_inavi_dumpplz_splash_overlay_500m.log` used `--registry .\dumpplz_regs.json`, the INAVI payload root, and `DUMPPLZ\FILES`; it reached the final idle stop with visible toolbar/icons in `frame_001_after_unicorn_dumpplz_splash_overlay_500m.png`.
- Polyline framebuffer probe `v2_synth_inavi_polyline_probe_500m.log` confirmed the red horizontal line at y=160 is app-drawn splash/progress UI, not a host blit artifact. The app selects a red pen and issues `Polyline` calls such as `(0,160)->(700,160)` on the paint DC after splash image blits.
- MSBuild succeeded after the synchronous `DestroyWindow` bridge and zero-area blit no-op fix. Clean bounded smoke `v2_synth_inavi_destroy_sync_clean_500m.log` opens disconnected guest `COM7:`, shows/tears down the serial failure dialog, destroys dialog/main windows through synchronous `WM_DESTROY`/`WM_NCDESTROY`, and exits with `UC_ERR_OK` instead of the previous `mfcce400.dll` null-this crash at `pc=0x50024e8c`. The final framebuffer has no red pixels on the old y=160 progress row, and the previous zero-area `BitBlt`/`StretchBlt` failure logs are gone.
- MSBuild and bounded smoke `v2_synth_inavi_rgb555_updatewindow_260m.log` succeeded after changing 16-bit DIB/bitmap conversion from the previous RGB565 assumption to SDK/Win32-style RGB555 for BI_RGB paths. A follow-up interactive report said the synchronous `UpdateWindow` experiment made startup black/late and caused icon glitches, so that continuation/presenter repaint experiment was reverted while keeping RGB555.
- MSBuild and bounded smoke `v2_synth_inavi_ui565_text.log` succeeded after replacing the blunt 16-bit bitmap assumption with per-bitmap masks from `CreateDIBSection`/`BI_BITFIELDS` and using CE-native RGB565 masks for the target's 16-bit DIB sections. The same fix removes the green/patterned icon blocks seen in the volume/statistic UI objects. Guest text rendering now disables host ClearType before copying GDI output into the framebuffer, removing the RGB subpixel fringes around Korean text in captured UI.
- MSBuild succeeded after the route-search/map-path cleanup that keeps one host presenter for the CE device framebuffer. Later top-level guest windows no longer create extra mirrored desktop presenter windows; they remain guest window records drawing into the same framebuffer.
- SDK COFF import-object evidence confirms the new route-search/map-path COREDLL ordinals: `#1023=_hypot`, `#2046=__gts`, and `#2052=__gtd`. They are now registered and implemented in the synthetic coredll math/soft-float bridge.
- Added a bounded host auto-driver at `tools/autodrive_inavi.ps1`. It launches the Windows GUI emulator, selects the `FakeCEHostPresenterWindow`, posts guest-coordinate mouse taps, and saves timestamped screenshots/logs under `captures\inavi_autodrive_*` without changing emulator behavior.
- Auto-driver run `captures/inavi_autodrive_20260522_003831` verified the harness can tap and screenshot the real presenter. With the multi-root filesystem layout, the app again opens/reads `INavi\mapdata\mapinfo.bin`, `cross\FullData.dat`, and many `MRData` files. The run also shows the map path creating/resuming guest worker threads at `0x000e6cd0` and `0x000e513c`, but the current `CreateThread`/`ResumeThread` bridge only returns guest handles and never executes those guest entry points.
- Final auto-driver run `captures/inavi_autodrive_20260522_004423` reached additional map/route code and exposed unsupported SDK-confirmed coredll ordinals `#1083=wcstoul` and `#2018=__f_to_ll`. Both are now registered and implemented as generic UTF-16 numeric parsing and soft-float-to-64-bit conversion helpers.
- Host presenter resizing now preserves an exact framebuffer-sized client area instead of applying guest `MoveWindow`/`SetWindowPos` dimensions to the host outer frame. This fixes the `784x441` resampling/stripe artifact seen in host captures while keeping guest coordinates at `800x480`.
- Destroyed guest HWNDs are now guarded in host-presenter creation and window shims (`FindWindowW`, `MoveWindow`, `SetWindowPos`, `ShowWindow`, `UpdateWindow`) so a stale post-destroy `ShowWindow` cannot create a second presenter for the same guest HWND.
- `tools/autodrive_inavi.ps1` now prefers the `FakeCEHostPresenterWindow` class before accepting a generic process window, avoiding accidental console/desktop screenshots during presenter verification.
- Verification `captures/inavi_autodrive_20260522_071728` with `--registry .\regs.json` created one presenter, returned `SystemParametersInfoW(0x102) = "iNavi GN 2010"`, used `800x480` client coordinates for all taps, and did not hit `can't read HWInfoDB`. Follow-up default-registry log `captures/inavi_autodrive_20260522_071922` confirmed restored `dumpplz_regs.json` also returns the same identity values and creates only one presenter.
- Host presenter repaint now uses immediate `RedrawWindow(... RDW_UPDATENOW ...)` plus `GdiFlush`, and `UpdateWindow` now synchronously dispatches guest `WM_ERASEBKGND` then `WM_PAINT` through a dedicated continuation instead of only queueing paint messages. Verification `captures/inavi_autodrive_20260522_072546` shows `UpdateWindow` entering synchronous erase/paint and `BeginPaint` occurring before `UpdateWindow` returns.
- MSBuild succeeded after pacing normal host-presenter invalidations to a 16 ms minimum and keeping forced presents for window lifecycle/update boundaries. Auto-driver runs `captures/inavi_autodrive_20260522_073408` and `captures/inavi_autodrive_20260522_073632` still show the first warning/splash paint arriving late, but the remaining delay is inside guest `UpdateWindow` erase/paint work rather than repeated host repaint calls.
- Splash/frame probes converted from the captured PPMs show the horizontal scanline pattern is already present in the decoded source bitmap before the host presenter copies the framebuffer. That artifact is not caused by host-window scaling; the host presenter is creating an exact `800x480` client for the primary CE framebuffer.
- Latest route/map auto-driver logs again open and read `INavi\mapdata` payloads, including `resi_800x480.bin`, `resmapi_800x480.bin`, `MRRank2.bin`, `MRVertex.bin`, and point/theme MDC data. The map still does not render because the route path creates guest worker threads, while the current `CreateThread`/`ResumeThread` bridge only creates/resumes synthetic handles and does not execute the guest entry points.
- MSBuild succeeded after adding nested child-window backing restore and parent-chain framebuffer origins for GDI drawing. Auto-driver run `captures/inavi_autodrive_20260522_082410` verifies that the destroyed warning dialog no longer remains stuck in the framebuffer: nested `TGNaviDlg` children restore their backing, the top-level splash child is not allowed to wipe the main surface with an old blank backing, and the final frame progresses to the real serial-port failure state instead of the stale safety warning.
- MSBuild succeeded after limiting `GetMessageW` timer fast-forwarding to headless/no-presenter runs. Auto-driver capture `captures/inavi_autodrive_20260522_135523` confirms the GUI presenter path now lets real host time drive timers: the safety full-screen window appears first (`0x00014006` at `13:56:07`), the smaller dialog appears later (`0x0001401f` at `13:56:09`), both tear down around `13:56:12`, and the process continues alive into later map-load activity instead of burning through the dialog timer immediately.
- Latest click-log diagnosis from the same capture shows host button/touch input was queued and handled by the bottom child window (`0x00010144`), including `SetCapture`, parent `SendMessageW(0x57e1)`, and `PostMessageW(0x5734)`, but repeated synchronous child `InvalidateRect` paints delayed later queued mouse messages. A follow-up patch makes the immediate child repaint shortcut yield when mouse input is already pending; alternate-output MSBuild succeeded because the normal Debug exe was still locked by the live emulator.
- Debug and Release MSBuild succeeded after the synthetic-DLL registration cleanup. Release now defaults spdlog to error level, debug builds keep env-selectable logging via `INAVI_EMU_LOG`, and framebuffer/bitmap dumps are disabled unless a debug build has `INAVI_EMU_DUMPS` enabled. `WINSOCK.dll`, `ole32.dll`, and `OLEAUT32.dll` registration and implementation moved into separate source files using generic ordinal-keyed handler maps; coredll audio registration moved into `coredll_audio.cpp` as a named subsystem handler group that still registers into the single synthetic `COREDLL.dll` module. Winsock no longer has a shared central switch handler: each ordinal maps to its own handler method, with name/code metadata retained in the ordinal spec.
- Debug and Release MSBuild succeeded after correcting the coredll audio split. The coredll audio subsystem now owns both registration and implementation in `coredll_audio.cpp`, with each wave/mixer ordinal mapped to its own handler method. The central coredll dispatch now invokes registered ordinal handlers before the generic coredll name-dispatch path, and the old central `dispatchWinmm` branch/function was removed.
- Debug and Release MSBuild succeeded after finishing the module-name plus ordinal handler lookup. Synthetic export stubs no longer store a handler pointer in each `ExportEntry`; dispatch resolves the DLL name to its registered DLL record (`name` plus integer-ordinal handler map), then looks up the ordinal and calls that handler. Coredll registry registration and implementation moved into `coredll_registry.cpp` as a separate subsystem group registered into the single synthetic `COREDLL.dll`, and the old central name-based `dispatchRegistryApi` path was removed.
- Debug and Release MSBuild succeeded after continuing the coredll subsystem split. `coredll_time.cpp` now owns time/performance ordinals (`SystemTimeToFileTime`, `FileTimeToSystemTime`, `GetLocalTime`, `GetSystemTime`, `GetTimeZoneInformation`, `Sleep`, `GetTickCount`, `QueryPerformanceCounter`, and `QueryPerformanceFrequency`) as a `coredll.time` ordinal group. `coredll_system.cpp` now owns last-error, bad-pointer, process-index, and process-dying helpers as a `coredll.system` ordinal group. Those names were removed from the central string-dispatch path.
- Debug and Release MSBuild succeeded after continuing the coredll split into `coredll_memory.cpp`. Local/heap/virtual/CRT/operator allocation ordinals now register through a `coredll.memory` ordinal group and no longer live in the central `dispatchGuestMemoryApi` string chain. The hardcoded ASM diagnostic hook addresses and `WINCE_ASM_DIAG` hook installer were removed from `main.cpp`.
- Debug and Release MSBuild succeeded after continuing the coredll split into `coredll_comm.cpp`, `coredll_rect.cpp`, and `coredll_thread.cpp`. Serial/COM APIs, rectangle helpers, and thread/event/mutex/wait-multiple helpers now register into the single synthetic `COREDLL.dll` as subsystem ordinal handler groups. The old duplicated COM branch, rectangle string branch, and thread/event/mutex central string branches were removed; dispatch resolves the DLL record by module name, then integer ordinal, then calls the per-ordinal handler.
- Debug and Release MSBuild succeeded after continuing the coredll split into `coredll_math.cpp`, `coredll_crt.cpp`, `coredll_fs.cpp`, `coredll_window.cpp`, `coredll_paint.cpp`, `coredll_gui.cpp`, and `coredll_res.cpp`. Math/soft-float, CRT stdio, filesystem, lightweight window state, paint/DC, GUI utility, and resource/menu/icon/image ordinals now register as subsystem groups into the single synthetic `COREDLL.dll` and are reached by integer ordinal lookup before any remaining central compatibility paths.
- Startup capture `captures/inavi_autodrive_20260522_155716` launched the Debug emulator after the GUI/resource split with taps disabled. The presenter window appeared, `00_initial.png` was captured, `emulator.stderr.log` was empty, and the guest stopped cleanly at `GetMessageW blocking with empty guest queue` with `UC_ERR_OK pc=0x70002ae8 ra=0x500245c8`. The only warning-class startup lines were expected synthetic-module substitutions for missing real CE DLLs plus a `FindFirstFileW` miss on an empty guest path; no crash or unsupported startup ordinal was logged.
- Route-helper investigation on 2026-05-24 confirmed additional Windows CE 4.2 Standard SDK MIPSII `coredll.lib` ordinals used by `happyway_win.exe`: `#186=GetACP`, `#209=IsValidLocale`, `#838=TranslateAcceleratorW`, and `#1146=vsprintf`. They are now registered and implemented in the synthetic coredll system/GUI/CRT groups. Standalone `happyway_win.exe` no longer reports those unsupported ordinals or crashes at `pc=0`; it opens the route/map `MRData` files and reaches an idle message wait.
- Host input routing now treats an enabled owned popup as a modal input root even when its owner/ancestor window is disabled. The previous route-search popup created `TGNaviDlg` as an owned popup under disabled `0x000140d4`; host clicks were incorrectly discarded at the disabled owner instead of being delivered to the visible modal.
